[tools]
cargo-release = "1.1.2"
hadolint = "2.14.0"
prek = "0.4.11"
shellcheck = "0.11.0"
tombi = "1.2.4"

[tasks.proptest]
description = "Run formatter property tests with a large case count"
env = { PROPTEST_CASES = "100000" }
run = "cargo test --test formatter_proptest"
